apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kambista-ingress
  namespace: mi-namespace
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: kambista-service
                port:
                  number: 80