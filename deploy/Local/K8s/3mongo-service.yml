apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: mi-namespace
  labels:
    app: mongo-app
spec:
  type: ClusterIP
  selector:
    app: mongo-app
  ports:
    - port: 27017
      targetPort: 27017