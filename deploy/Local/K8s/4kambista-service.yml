apiVersion: v1
kind: Service
metadata:
  name: kambista-service
  namespace: mi-namespace
  labels:
    app: kambista-app
spec:
  type: ClusterIP
  selector:
    app: kambista-app
  ports:
    - name: http
      port: 80
      targetPort: 80