apiVersion: v1
kind: Service
metadata:
  name: currency-exchange-lb
spec:
  selector:
    app: currency-exchange
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
